Different semantic classes require different thresholds.

Examples:
- character tags → high threshold
- environment tags → medium threshold
- motion tags → adaptive threshold

Avoid global confidence thresholds.